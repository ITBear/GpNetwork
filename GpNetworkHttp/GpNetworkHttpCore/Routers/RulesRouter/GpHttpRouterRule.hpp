#pragma once

#include "../../RequestHandlers/GpHttpRequestHandlerFactory.hpp"

namespace GPlatform {

/*class GP_NETWORK_HTTP_CORE_API GpHttpRouterRule
{
public:
    CLASS_REMOVE_CTRS_MOVE_COPY(GpHttpRouterRule)
    CLASS_DD(GpHttpRouterRule)


public:
                                        GpHttpRouterRule    (void) noexcept;
                                        ~GpHttpRouterRule   (void) noexcept;

    GpHttpRequestHandler::SP            OnRequest           (const GpHttpRequestNoBodyDesc& aHttpRqNoBody) const;

private:
    ?
    s_int_64                            iPriority   = 0;
    GpHttpRouterRuleMatcher::CSP        iMatcher;
    GpHttpRouterRule::C::Vec::CSP       iChildRules;
};*/

}// namespace GPlatform
