#pragma once

#include "../GpNetwork_global.hpp"

namespace GPlatform {

GP_ENUM(GPNETWORK_API, GpHttpContentType,
    TEXT_PLAIN,
    APPLICATION_JSON,
    APPLICATION_XML
);

}//namespace GPlatform
