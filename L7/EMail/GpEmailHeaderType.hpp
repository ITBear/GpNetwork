#pragma once

#include "../../GpNetwork_global.hpp"

namespace GPlatform {

GP_ENUM(GPNETWORK_API, GpEmailHeaderType,
    CC,
    CONTENT_TRANSFER_ENCODING,
    CONTENT_TYPE,
    DATE,
    FROM,
    LIST_UNSUBSCRIBE,
    MESSAGE_ID,
    MIME_VERSION,
    SUBJECT,
    TO
);

}//namespace GPlatform
