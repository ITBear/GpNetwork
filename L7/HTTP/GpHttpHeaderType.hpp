#pragma once

#include "../../GpNetwork_global.hpp"

namespace GPlatform {

GP_ENUM(GPNETWORK_API, GpHttpHeaderType,
    CONTENT_TYPE,
    CONTENT_LENGTH,
    CONNECTION,
    CACHE_CONTROL
);

}//namespace GPlatform
